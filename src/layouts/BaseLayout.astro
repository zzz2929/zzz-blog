---
import { AnimatedBackground } from "@/components/react/AnimatedBackground";
import MiniPlayer from "@/components/astro/MiniPlayer.astro";
import Analytics from "@/components/astro/Analytics.astro";
import { getLocaleFromURL } from "@/i18n";

interface Props {
  title?: string;
  description?: string;
  image?: string;
}

const locale = getLocaleFromURL(Astro.url.pathname);
const descriptionMap: Record<string, string> = {
  en: "zzz's personal blog",
  "zh-TW": "zzz 的個人部落格",
  "zh-CN": "zzz 的个人博客",
};
const {
  title = "zzz-blog",
  description = descriptionMap[locale],
  image,
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const siteTitle = title === "zzz-blog" ? title : `${title} | zzz-blog`;
const isMusicPage =
  Astro.url.pathname === "/music/" || Astro.url.pathname === "/music";
---

<!doctype html>
<html lang={locale}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" href="/favicon.webp" />

    <meta name="generator" content={Astro.generator} />

    <!-- SEO -->
    <title>{siteTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={siteTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:site_name" content="zzz-blog" />
    {image && <meta property="og:image" content={new URL(image, Astro.site)} />}

    <!-- Twitter Card -->
    <meta
      name="twitter:card"
      content={image ? "summary_large_image" : "summary"}
    />
    <meta name="twitter:title" content={siteTitle} />
    <meta name="twitter:description" content={description} />
    {
      image && (
        <meta name="twitter:image" content={new URL(image, Astro.site)} />
      )
    }

    <!-- Dark mode inline script (must run before body to avoid FOUC) -->
    <script is:inline>
      (function () {
        const stored = localStorage.getItem("theme");
        const prefersDark = window.matchMedia(
          "(prefers-color-scheme: dark)",
        ).matches;
        if (stored === "dark" || (!stored && prefersDark)) {
          document.documentElement.classList.add("dark");
        }
      })();
    </script>
    <Analytics />
  </head>
  <body
    class="min-h-screen bg-background text-foreground font-sans antialiased"
  >
    <AnimatedBackground client:only="react" />
    <slot />
    {!isMusicPage && <MiniPlayer />}

    <!-- Image loading placeholder (uiverse mobinkakei/grumpy-turtle-41) -->
    <script is:inline>
      (function () {
        function mount(img) {
          if (img.dataset.imgLoader) return;
          img.dataset.imgLoader = "1";
          if (img.complete && img.naturalWidth > 0) return;
          var parent = img.parentElement;
          if (!parent) return;
          var overlay = document.createElement("div");
          overlay.className = "img-loading-overlay";
          overlay.innerHTML =
            '<div class="pl-wrapper"><div class="pl-circle"></div><div class="pl-circle"></div><div class="pl-circle"></div><div class="pl-shadow"></div><div class="pl-shadow"></div><div class="pl-shadow"></div></div>';
          var pr = parent.getBoundingClientRect();
          var ir = img.getBoundingClientRect();
          if (getComputedStyle(parent).position === "static") {
            parent.style.position = "relative";
            pr = parent.getBoundingClientRect();
          }
          overlay.style.left = ir.left - pr.left + "px";
          overlay.style.top = ir.top - pr.top + "px";
          overlay.style.width = ir.width + "px";
          overlay.style.height = ir.height + "px";
          var scale = Math.min(
            1,
            ir.width / 200 || 0,
            ir.height / 60 || 0,
          );
          if (scale > 0 && scale < 1) {
            overlay.style.setProperty("--pl-scale", scale.toFixed(3));
          }
          parent.appendChild(overlay);
          img.__plOverlay = overlay;
          var done = function () {
            if (!overlay.parentNode) return;
            overlay.classList.add("done");
            setTimeout(function () {
              overlay.remove();
            }, 250);
          };
          img.addEventListener("load", done);
          img.addEventListener("error", done);
          if (img.complete) done();
        }
        function scan(root) {
          (root || document)
            .querySelectorAll("img")
            .forEach(mount);
        }
        if (document.readyState === "loading") {
          document.addEventListener("DOMContentLoaded", function () {
            scan();
          });
        } else {
          scan();
        }
        // Images added later (filters, dynamic panels) get placeholders too;
        // removed images take their placeholder with them
        var cleanupRemoved = function (node) {
          if (node.tagName === "IMG") {
            if (node.__plOverlay) {
              node.__plOverlay.remove();
              node.__plOverlay = null;
            }
          } else if (node.querySelectorAll) {
            node.querySelectorAll("img").forEach(function (img) {
              if (img.__plOverlay) {
                img.__plOverlay.remove();
                img.__plOverlay = null;
              }
            });
          }
        };
        var observer = new MutationObserver(function (mutations) {
          mutations.forEach(function (m) {
            m.addedNodes.forEach(function (node) {
              if (!node.tagName) return;
              if (node.tagName === "IMG") mount(node);
              else if (node.querySelectorAll) scan(node);
            });
            m.removedNodes.forEach(function (node) {
              if (node.tagName) cleanupRemoved(node);
            });
          });
        });
        observer.observe(document.documentElement, {
          childList: true,
          subtree: true,
        });
      })();
    </script>
  </body>
</html>

<style is:global>
  @import "@/styles/global.css";

  /* ===== Image loading placeholder ===== */
  .img-loading-overlay {
    position: absolute;
    z-index: 5;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    border-radius: 8px;
    background: rgba(0, 0, 0, 0.04);
    pointer-events: none;
    transition: opacity 0.25s ease;
  }
  .img-loading-overlay.done {
    opacity: 0;
  }
  .dark .img-loading-overlay {
    background: rgba(255, 255, 255, 0.06);
  }
  .img-loading-overlay .pl-wrapper {
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%) scale(var(--pl-scale, 1));
  }

  .pl-wrapper {
    width: 200px;
    height: 60px;
    position: relative;
    z-index: 1;
  }

  .pl-circle {
    width: 20px;
    height: 20px;
    position: absolute;
    border-radius: 50%;
    background-color: var(--color-primary);
    left: 15%;
    transform-origin: 50%;
    animation: pl-circle7124 0.5s alternate infinite ease;
  }

  @keyframes pl-circle7124 {
    0% {
      top: 60px;
      height: 5px;
      border-radius: 50px 50px 25px 25px;
      transform: scaleX(1.7);
    }

    40% {
      height: 20px;
      border-radius: 50%;
      transform: scaleX(1);
    }

    100% {
      top: 0%;
    }
  }

  .pl-circle:nth-child(2) {
    left: 45%;
    animation-delay: 0.2s;
  }

  .pl-circle:nth-child(3) {
    left: auto;
    right: 15%;
    animation-delay: 0.3s;
  }

  .pl-shadow {
    width: 20px;
    height: 4px;
    border-radius: 50%;
    background-color: rgba(0, 0, 0, 0.35);
    position: absolute;
    top: 62px;
    transform-origin: 50%;
    z-index: -1;
    left: 15%;
    filter: blur(1px);
    animation: pl-shadow046 0.5s alternate infinite ease;
  }

  @keyframes pl-shadow046 {
    0% {
      transform: scaleX(1.5);
    }

    40% {
      transform: scaleX(1);
      opacity: 0.7;
    }

    100% {
      transform: scaleX(0.2);
      opacity: 0.4;
    }
  }

  .pl-shadow:nth-child(4) {
    left: 45%;
    animation-delay: 0.2s;
  }

  .pl-shadow:nth-child(5) {
    left: auto;
    right: 15%;
    animation-delay: 0.3s;
  }

  .dark .pl-circle {
    background-color: var(--color-primary-dark);
  }
</style>
